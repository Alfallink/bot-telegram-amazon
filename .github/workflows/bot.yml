# Passo extra: Testar conexão com Shopee
- name: Testar conexão API Shopee
  run: |
    echo "Testando conexão com Shopee..."
    curl -s -o /dev/null -w "Status: %{http_code}" "https://shopee.com.br/api/v4/search/search_items?keyword=iphone&limit=1"
    echo ""
    curl -s "https://shopee.com.br/api/v4/search/search_items?keyword=iphone&limit=1" | head -c 200
    echo "..."
